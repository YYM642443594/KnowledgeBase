# AGENTS.md — AI Agent 工作规范

> 本文件面向 AI 编码助手（Claude Code、Codex、Cursor 等）。任何 agent 在本仓库内新增、修改、移动文档前，必须遵循以下规则。面向人类的完整规范见 [README.md](README.md)。

## 仓库概况

- 这是一个**纯 Markdown 知识库**：没有代码、没有构建系统、没有测试，不要尝试 `npm install` / `make` 等操作。
- 文档语言一律使用**简体中文**（代码、命令、专有名词除外）。
- 三个关键文件：
  - [README.md](README.md) — 人类向完整规范
  - [00-索引/索引.md](00-索引/索引.md) — 全库唯一索引，**每篇文档都必须登记在这里**
  - [00-索引/模板-知识文档.md](00-索引/模板-知识文档.md) — 新建文档的唯一模板
- **每次会话必须执行「开发会话工作流」**：开始时 `git pull` 并读取索引，结束时整理知识并 `git push`（见下文）。

## 开发会话工作流（每次必做）

知识库内容统一在 **main 分支**维护。每次开发会话遵循「先读取、后沉淀」的闭环：

### 会话开始：从知识库读取

1. **同步远程**：`git pull --rebase origin main`；有冲突先解决再继续，无法自动解决则如实告知用户；
2. **读索引**：读取 [00-索引/索引.md](00-索引/索引.md)，了解全库已有文档；
3. **查背景**：根据本次任务主题，检索并阅读相关已有文档（技术笔记 / 项目文档 / 问题排查），复用已有结论，避免重复踩坑、重复写作。

### 会话结束：整理并上传

功能完成、验证通过后，在结束会话前完成知识沉淀：

1. **判定沉淀内容**（对照下表，确实无则可跳过并说明）：

   | 本次工作产出 | 沉淀方式 |
   |------|------|
   | 项目的设计、方案、复盘 | `02-项目文档/` 新文档 |
   | 踩坑、排障过程 | `03-问题排查/` 新文档（现象→原因→解决方案→预防措施） |
   | 新掌握的技术 / 命令 / 配置 | `01-技术笔记/` 或 `04-工具使用/` 新文档，或更新已有文档 |
   | 对已有文档的修正 | 原地更新，并改 front-matter 的 `updated` 字段 |

2. 按「新增文档流程」写文档并登记索引；
3. 检查 `git status`，只添加本次有意变更的文件，勿把无关文件一并提交；
4. **提交并推送**：

   ```bash
   git add <本次变更的文件>
   git commit -m "新增：<文档标题>"   # 前缀按实际情况：新增：/ 更新：/ 整理：
   git push origin main
   ```

5. push 失败（凭证失效、网络不通等）时，**保留本地提交并如实告知用户**，不要丢弃或强行重写改动。

## 新增文档流程（必须完整执行）

1. **选目录**：按内容选择唯一分类（见下表），拿不准时选最接近的并在索引中注明；
2. **复制模板**：以 `00-索引/模板-知识文档.md` 为基础创建文件，填写 YAML front-matter（`title` / `created` / `updated` / `tags` / `source`）；
3. **命名**：语义清晰的中文名；不含空格和 `/ \ : * ? " < > |`；带日期的记录类文档以 `YYYY-MM-DD-` 开头；
4. **登记索引**：在 `00-索引/索引.md` 对应分类标题下添加一行，格式见该文件内的注释；
5. **提交**：`git add -A && git commit && git push`，提交信息用前缀 `新增：` / `更新：` / `整理：` / `归档：` + 文档标题。

## 目录选择

| 目录 | 放什么 |
|------|--------|
| `01-技术笔记/` | 技术学习笔记（按 `编程语言/前端/后端/数据库/运维与部署/AI与数据` 细分） |
| `02-项目文档/` | 项目的设计、方案、复盘 |
| `03-问题排查/` | Bug 记录、报错与解决、踩坑（结构：现象→原因→解决方案→预防措施） |
| `04-工具使用/` | 软件安装配置、使用技巧 |
| `05-读书笔记/` | 书籍 / 课程 / 文章笔记 |
| `06-参考资料/` | 外部资料摘录（**必须注明原文出处与链接**） |
| `07-日常记录/` | 日志、备忘、灵感（文件名以日期开头） |
| `90-归档/` | 过时文档（agent **不要主动**移入或修改，除非用户要求） |

## 硬性规则

- 一篇文档只放在**一个**目录，跨主题用索引和相对链接关联，**禁止多处复制**。
- 新增、移动、重命名文档后，**必须同步更新** `00-索引/索引.md`；移动 / 重命名时必须修正所有引用该文档的相对链接。
- 文档内容遵循「先结论，后细节」；问题排查类必须包含「现象 → 原因 → 解决方案 → 预防措施」。
- 转载 / 摘录内容必须在 front-matter 的 `source` 注明出处，正文附原文链接。

## 链接与资源

- 文档间链接一律用**相对路径**：`[说明](../01-技术笔记/后端/xxx.md)`。
- 图片放 `assets/images/`，其他附件放 `assets/files/`，命名建议「所属文档名-说明」；引用同样用相对路径：`![说明](../assets/images/xxx.png)`。
- **禁止**在文档中使用本机绝对路径（如 `/home/...`）。
- **禁止**提交超过 10 MB 的二进制文件；图片优先 PNG / SVG，能压缩则压缩。

## 边界

- 不要修改 `.gitignore`、`AGENTS.md`、`CLAUDE.md`，除非用户明确要求。
- `.claude/` 目录是本地个人配置，已被 gitignore，不要在其中创建会被提交的文件。
- 结构性调整（增删顶级目录、变更命名规则）完成后，必须同步更新 `README.md` 的目录结构与对应章节。
